*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/docs

# Module build outputs (root /build above only covers the root)
**/build/
.kotlin/

generated-images/

# Release signing — never commit these
/keystore/
/keystore.properties
*.jks
*.keystore

# Machine-local IDE/editor state — never project source
/.idea/
*.local-bak
*.disabled-by-claude

# Assistant session notes, rewritten automatically by a local tooling plugin. They were removed from
# the repo at the Owner's decision (2026-09-18) but re-added by accident in e4f43f5 and pushed;
# ignoring them keeps the plugin's rewrites out of the public history (2026-09-25 audit).
/AGENTS.md
/CLAUDE.md
