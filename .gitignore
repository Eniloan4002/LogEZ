*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/docs

# Module build outputs (root /build above only covers the root)
**/build/
.kotlin/

generated-images/

# Release signing — never commit these
/keystore/
/keystore.properties
*.jks
*.keystore

# Machine-local IDE/editor state — never project source
/.idea/
*.local-bak
*.disabled-by-claude

# Stale nested clone of this repo (pre-share-feature history), left over from the
# 2026-08-27 history unification; this outer repo is the canonical GitHub-connected
# one. Ignored until the Owner deletes the folder.
/LogEZ/
