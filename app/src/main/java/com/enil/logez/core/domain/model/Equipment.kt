package com.enil.logez.core.domain.model

/** PHASE2_PLAN.md §3.1 — all 9 values; every one appears on at least one seed exercise (§7.1). */
enum class Equipment {
    NONE,
    BARBELL,
    DUMBBELL,
    KETTLEBELL,
    MACHINE,
    PLATE,
    RESISTANCE_BAND,
    SUSPENSION,
    OTHER,
}
