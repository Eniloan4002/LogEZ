package com.enil.logez.core.domain.model

/** Which fields the Measurements screen shows for entry/display — edited from that screen itself,
 * not the Settings tree (same precedent as the per-exercise weight-unit override). */
enum class MeasurementsTrackingMode {
    SIMPLIFIED,
    COMPLETE,
}
