package com.enil.logez.core.domain.model

/** PHASE2_PLAN.md §3.1 §8.10. Governs only the PREVIOUS column's source, never routine targets. */
enum class PreviousValuesMode {
    ANY_WORKOUT,
    SAME_ROUTINE,
}
