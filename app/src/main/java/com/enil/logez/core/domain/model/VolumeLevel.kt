package com.enil.logez.core.domain.model

/** PHASE2_PLAN.md §5.2 — shared by the three independent Timer/Set-Complete/PR volume settings. */
enum class VolumeLevel {
    OFF,
    LOW,
    NORMAL,
    HIGH,
}
