package com.enil.logez.feature.activity

/** M21a. State lives in the shared `ActivityTrackingController` singleton, not the nav back stack — no path args needed. */
object ActivityTrackingRoutes {
    const val LIVE_TRACKING = "activity_tracking_live"
}
